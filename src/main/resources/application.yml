spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vacation_planner
    username: vacation_planner
    password: vacation_planner

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL8Dialect

